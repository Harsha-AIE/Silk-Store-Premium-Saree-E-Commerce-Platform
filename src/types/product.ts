export type Product = {
  id: string;
  name: string;
  category: string;
  price: string;
  fabric: string;
  occasion: string;
  color: string;
  imageId: string;
  description: string;
};

export type Category = {
  name: string;
  imageId: string;
};
